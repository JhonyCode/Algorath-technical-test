{% extends 'base.html.twig' %}

{% block title %}Relations{% endblock %}

{% block body %}
    <h1>Relations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ relation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_relations_index') }}">back to list</a>

    <a href="{{ path('app_relations_edit', {'id': relation.id}) }}">edit</a>

    {{ include('relations/_delete_form.html.twig') }}
{% endblock %}
